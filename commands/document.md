Write documentation for the following code or feature.

Target: $ARGUMENTS

First, read the relevant source files to understand the code thoroughly.

Produce documentation appropriate to the target:
1. **Usage docs**: How to use the feature with concrete examples (CLI commands, API calls, function usage)
2. **Inline comments**: Only where the WHY is non-obvious — suggest specific file:line locations
3. **API docs**: If applicable, document endpoints, parameters, return values, and error codes
4. **Configuration**: Any config options, env vars, or flags the feature introduces

Keep docs concise and practical. Focus on what a developer needs to USE the code. Don't explain obvious implementation details. Write for someone who reads code fluently but doesn't know this codebase.
