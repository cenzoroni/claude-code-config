Write tests for the following code or feature.

Target: $ARGUMENTS

First, read the relevant source files and any existing tests to understand:
- The testing framework and patterns used in this project
- What's already tested and what's missing
- The code under test

Then write tests covering:
1. **Happy path**: Normal expected usage with valid inputs
2. **Edge cases**: Boundary conditions, empty inputs, zero values, max values
3. **Error cases**: Invalid inputs, missing dependencies, failure modes

Follow the project's existing test conventions and file naming patterns. Keep tests focused — one behavior per test. Use descriptive test names that explain what's being verified.

Run the tests after writing them to verify they pass.
