Implement the following feature or change.

Request: $ARGUMENTS

Before writing code:
1. Read CLAUDE.md for project conventions and constraints
2. Read the relevant source files to understand existing patterns
3. If a spec or architecture doc exists for this feature, read it first

Implementation rules:
- Make minimal, targeted changes — don't refactor unrelated code
- Follow the project's existing patterns and naming conventions
- No hardcoded secrets — use env vars or config files
- Don't add comments unless the WHY is non-obvious
- Don't add error handling for impossible scenarios
- Build and verify the code compiles before reporting done

After implementing, run any existing tests to check for regressions.
